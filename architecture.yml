owner: ucphhpc
architecture:
  ssh-mount-dummy:
    arm64:
      parent:
        owner: arm64v8
        image: debian
        tag: latest
        pipeline_dependent: false
    amd64:
      parent:
        image: debian
        tag: latest
        pipeline_dependent: false
    